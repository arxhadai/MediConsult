/// Enum representing the role of a participant in a video call
enum ParticipantRole {
  patient,    // Patient participant
  doctor,     // Doctor participant
  admin,      // Admin/observer participant
}